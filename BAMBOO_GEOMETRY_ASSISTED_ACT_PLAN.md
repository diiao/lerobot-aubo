# 竹条中点几何感知与 ACT 增强计划书

> 状态：**方案与审核完成，尚未实施。**
>
> 建立日期：2026-08-02
>
> 范围：AUBO i10、LeRobot、当前 `bamboo_newview_full` 数据集，以及手机遥操采集方式。
> 本文不授权启动机械臂、录制、训练或修改控制逻辑。

## 1. 要解决的实际问题

当前任务要求吸盘尽量抓住竹条中部。手机遥操可以提供完整的“接近—下压—吸取—搬运—放置”示教，但操作者手动对中不可避免有偏差。因此，不能把“人实际吸到了哪里”直接等同于“竹条理想中点”。

纯 ACT 能从图像中**隐式**学习竹条位置，但它没有被显式要求输出端点或中点；在当前 30 条、竹条摆放变化较小的数据上，不能将较低验证损失解释为稳定的毫米级几何定位。ACT 原论文也指出，精细操作会受到示教非一致性和策略误差累计的影响；其 action chunking 是缓解时序问题的机制，不是几何测量器 [1]。

本项目将中点定义为竹条两端物理坐标的平均：

\[
C_B=(P_{B1}+P_{B2})/2
\]

并使用单位方向向量：

\[
d_B=(P_{B2}-P_{B1})/\|P_{B2}-P_{B1}\|
\]

其中下标 `B` 表示机器人基座坐标系。最终抓取点不是盲目等于 `C_B`：应在完成接触工具几何测量后，明确配置吸盘中心相对中点的偏移；此偏移必须可记录、可审查，不能隐藏在推理代码中。

## 2. 当前系统边界与关键结论

| 项目事实 | 对方案的影响 |
|---|---|
| `handeye` 是眼在手外的全局相机；`fixed` 是近景相机 | 第一阶段只用 `handeye` 定位竹条；`fixed` 仅用于人工复核/后续接触验证，不能假定两者已完成立体标定。 |
| 输入 `observation.state` 目前为 13 维，ACT 同时接收两路 640×480 图像 | 当前 ACT 实现原生消费单个 `observation.state`；第一期应将几何量按固定顺序追加为 19 维，并在数据元信息/报告中记录每一维含义。训练、录制和推理的 feature 定义必须完全一致。 |
| action 是绝对末端目标，25 Hz；现有安全边界仍须保留 | 不能把几何方案实现成覆盖 ACT 动作的固定 xyz 抓取脚本，更不能移除工作空间、单步位移和姿态连续性保护。 |
| 当前 30 条数据用于纯 ACT 首个基线 | 基线数据、checkpoint 和评估结果必须保留；几何增强实验使用新的数据集和模型目录，不能混称为“纯 ACT”。 |

**核心判断：**“把中点状态输入 ACT”可以降低它从少量图像中自行推断几何的难度，但不能自动消除手机遥操动作标签中的偏差。感知误差、标定误差、吸盘接触误差和示教误差必须分开测量。

### 通俗版：后续到底怎么做

可以把系统理解为给 ACT 增加一位“拿尺子的观察员”。这位观察员不操作机械臂，只负责告诉 ACT：竹条的两头在哪里、中心在哪里、方向朝哪里；ACT 仍然负责学习完整动作流程。

```text
现在：     图像 + 机械臂姿态 -> ACT 猜动作
以后：     图像 -> 几何模块量出中点/方向
          图像 + 机械臂姿态 + 中点/方向 -> ACT 猜动作
```

执行顺序不是立刻重训，而是：

1. **保留当前纯 ACT。** 它是对照组，用来知道“没有几何信息时”实际能做到什么；不会被覆盖。
2. **先让相机指出中点。** 在画面上画出竹条两端、连线和中点，先由人判断是否画对；此时机械臂完全不动。
3. **再建立坐标地图。** 标定把“图片中的像素位置”换算成“机械臂基座下的米/毫米位置”。
4. **先量精度，再接入训练。** 竹条摆在独立测试位置，用中心标记或量具核对相机预测是否够准；不准就改感知/标定，不把错误数字交给 ACT。
5. **新录一套几何增强数据。** 你的手机仍负责示教接近、下压、吸取、搬运和放置；每帧额外记录客观中点和方向。这样不要求你每次都手动吸在完美中点。
6. **公平重训和比较。** 原纯 ACT 与“增加几何状态的 ACT”在同一批独立测试摆放上比较抓取偏差和成功率；只有后者确实更好才保留。
7. **最后才考虑按键自动对中。** 它是独立的共享自治功能，不是纯 ACT，也不属于第一期工作。

这套顺序的要点是：**先证明相机量得对，再训练模型使用这个量；不能先把一个未经验证的中点喂进模型。**

## 3. 技术路线比较与推荐

| 路线 | 做法 | 优点 | 局限/风险 | 结论 |
|---|---|---|---|---|
| A. 继续纯 ACT | 仅图像 + 13 维机器人状态 | 最简单，维持现有基线定义 | 中点由网络隐式学习；小数据下不可解释，无法独立定位误差来源 | 必须保留为对照组，但不满足精准中点目标。 |
| B. **几何状态增强 ACT** | 视觉模块输出中点、方向、置信度，作为 ACT 的额外 observation | 保留 ACT 的端到端时序策略；几何量可视化、可评估；不要求人每次精确对中 | 需要相机/桌面标定和新数据；ACT 仍可能模仿示教偏差 | **推荐主线。** 先做只读感知验证，再做独立训练实验。 |
| C. 视觉伺服自动对中 | 在接触前由几何闭环直接移动 TCP 到中点 | 对中精度上限最高，物理含义最清楚 | 属于任务专用控制器，改变当前“纯 ACT”定义；需要严格安全状态机和现场验证 | 作为 B 通过后另立“共享自治”对照实验，不能悄悄加入纯 ACT 推理。 |
| D. RGB-D/双目 3D 感知 | 增加深度相机或完成双目同步标定 | 可处理离桌面、翘起和遮挡 | 硬件、标定和时序复杂度明显提高；当前 RGB 两相机并非已标定双目 | 暂不作为第一期；只有平面假设失效时再评估。 |

路线 B 的对象关键点表示与 kPAM 的思想一致：用任务相关关键点来表达物体，而不是强制拟合完整 6D 物体模型 [2]。路线 C 属于视觉伺服，即把视觉测量放入闭环控制；其稳定性和误差分析不能省略 [3]。

## 4. 推荐架构：先测量，后条件化，不直接接管机械臂

```text
handeye RGB 图像
  -> 竹条分割/端点检测
  -> 像素端点 (u1,v1), (u2,v2) 与置信度 q
  -> 相机标定 + 像素射线与桌面平面相交
  -> 基座系 P_B1, P_B2
  -> 几何状态 g=[C_B.x,C_B.y,C_B.z,d_B.x,d_B.y,q]
  -> 记录/推理时拼接为 observation.state=[原13维 | g]（共19维）
  -> ACT（原图像 + 原13维状态 + g）
  -> 原有通用安全边界 -> AUBO i10
```

### 4.1 为什么优先采用“RGB + 已知桌面平面”

竹条在本任务中预期平放于固定桌面。对已标定的单目相机，每个像素对应一条三维射线；射线与已知桌面平面相交即可得到其三维位置。因此第一期不需要假装拥有可靠深度，也不需要立即引入新硬件。相机内参、畸变和外参是该计算成立的前提；Zhang 的标定方法是这一类度量视觉流程的经典基础 [4]，OpenCV 的 `calibrateCamera`/`solvePnP` 文档给出了工程接口和坐标约定 [5]。

如果竹条明显翘起、弯曲、被吸盘严重遮挡，平面假设就会带来系统偏差；这时应停止把输出当作精确三维量，转向 RGB-D 或经同步/外参验证的双目方案，而不是用经验偏移掩盖问题。

### 4.2 几何状态的第一版定义与向量顺序

为避免方向的 180° 二义性，第一版不直接记录角度 `theta`，而记录方向的双角形式：

```text
geometry_bamboo（追加到 `observation.state` 的末 6 维）= [
  center_x_m, center_y_m, center_z_m,
  cos(2*theta), sin(2*theta),
  visible_confidence
]
```

- 新 state 的完整顺序为：`[原有13维, center_x_m, center_y_m, center_z_m, cos(2*theta), sin(2*theta), visible_confidence]`。数据集元信息、推理日志和可视化必须写出这一顺序，不能只写“19 维状态”。
- `center_*_m`：机器人基座坐标，单位米；`center_z_m` 在平面假设下接近常数，但保留以便明确坐标系和后续升级。
- `cos(2*theta), sin(2*theta)`：竹条没有“左端/右端”的语义，`theta` 与 `theta+pi` 应代表同一根竹条；双角编码可避免端点顺序反转造成状态突变。
- `visible_confidence`：由检测器质量、掩码面积、端点几何一致性和时间稳定性构成，范围 `[0,1]`。低置信度不是“中点为零”，而是一个必须被记录和评估的失效状态。

初期只用这 6 维；不要一开始加入大量难以验证的“长度、颜色、深度、历史轨迹”等特征。若后续需要长度，可在端点误差通过验收后单独加入。

### 4.3 不同长度竹条：第二阶段再引入的长度泛化方案

不同长度不改变中点定义：只要两端可见，`C_B=(P_B1+P_B2)/2` 仍然成立。但长度会影响视觉尺度、碰撞余量、搬运时的摆动以及放置空间；若希望模型跨长度工作，不能只用一种长度的数据训练后直接宣称泛化。

因此长度变化采用“一个变量一次”的顺序：

```text
G1：固定一种长度 -> 验证端点、中点、方向、标定和 19 维几何增强 ACT
G2：G1 已通过 -> 加入短/中/长竹条和 length_m -> 独立训练、独立测试
```

在 G2 中，感知模块额外计算桌面平面内的物理长度：

\[
L=\|P_{B2}-P_{B1}\|
\]

并将新状态扩为 20 维：

```text
[原有13维,
 center_x_m, center_y_m, center_z_m,
 cos(2*theta), sin(2*theta), visible_confidence,
 length_m]
```

`length_m` 是端点在基座系/桌面平面中的距离，不是图像里的像素长度；只有完成标定后才有可靠物理意义。20 维状态对应新的数据集、模型目录和推理配置，绝不能拿去加载当前 13 维纯 ACT 模型或 19 维 G1 模型。

G2 的数据与验收要求：

1. **先定义范围。** 明确本任务允许的最短、最长竹条及短/中/长分档边界；长度超出范围时应拒绝，而不是让模型盲目外推。
2. **检查完整可见性。** 每种长度、每个允许角度下，`handeye` 必须同时看见两端；任何一端出画、严重遮挡或与背景混淆时输出 `INVALID`，不能由半截竹条猜中点或长度。
3. **受控采集。** 在保持相机、桌面、起始位、光照和任务流程不变的前提下，再改变长度；不要同时扩大位置范围、换光源和换竹条材质。
4. **均衡示教。** 每个长度档都采集成功、动作连贯且抓取偏差已标注的示教；不能让某一长度档占绝大多数，否则模型可能只偏向常见长度。
5. **保留长度外测试。** 训练时至少保留一个长度区间或若干具体长度不参与训练，用于检验跨长度能力；报告必须分短/中/长，而不是只给总成功率。
6. **独立比较。** 比较“19 维、固定长度 G1”与“20 维、长度变化 G2”，同时记录中点误差、长度误差、抓取偏差、碰撞/出画次数和成功率。若 G2 未提升，不应以更复杂模型替代 G1。

这一步的目标不是让 ACT 记住某个像素大小，而是让它根据“中点、无向方向、物理长度”理解当前竹条的尺度。当前 action 仍保持绝对末端目标定义；若未来要改为相对竹条坐标动作，应另立实验，不能和长度实验同时改动。

### 4.4 第二步的具体做法：在图像上画几何叠加线（只读预览）

这一小步的目的不是控制 AUBO，而是回答一个非常直观的问题：**系统声称的中点，是否真的落在竹条中部？**

当前 `diag_preflight_cams.py` 已能以不连接机械臂的方式读取 `handeye` 并保存 640×480 预览图到 `/tmp/aubo_camera_preflight/handeye.jpg`。未来的几何预览工具应复用同一物理相机约定和只读方式；它不导入机器人控制、不建立 AUBO 连接、不发送 action。

#### 输入与输出约定

每个待检查画面应产生一份检测记录：

```text
输入：frame_id、timestamp、handeye 原始 640×480 图像
检测：mask、P1=(u1,v1)、P2=(u2,v2)、q、reject_reason
绘制：overlay PNG（原图上叠加）+ 同名 JSON（上述数值与算法版本）
```

- 像素坐标约定：`u` 向右增加，`v` 向下增加；`P1/P2` 没有左右语义，交换它们不应改变中点或方向轴。
- 当前相机预检读到的是 RGB 帧，写入 OpenCV 文件时会显式转为 BGR。未来实现必须在接口处写明颜色空间，保证“用于检测的图像”和“用于绘制的图像”来自同一帧，避免 RGB/BGR 混淆或绘制到缩放后的错误坐标。
- 第一轮叠加只在**像素坐标**中工作。画对像素中点不等于已经画对机械臂坐标；毫米精度必须等阶段 1 标定完成后再评价。

#### 每一帧的具体步骤

1. **取得原始图像。** 优先从历史 `handeye` 视频抽帧，离线处理；确认稳定后，才可使用只读实时相机预览。保持原始 640×480，不裁剪、不缩放后再拿旧坐标绘图。
2. **检测竹条。** 感知模块输出竹条掩码，或直接输出两个端点 `P1/P2` 和置信度 `q`。原型阶段可以人工标注端点，以便先验证“绘制/坐标逻辑”本身；不要一上来把检测错误和绘制错误混在一起。
3. **先做合法性检查。** 检查端点在图像内、两端距离大于最小可见长度、掩码面积合理、`q` 有效。任一不满足时标记为 `invalid`，不计算或沿用旧中点。
4. **计算几何量。** 对合法端点计算像素中点 `C_px=((u1+u2)/2,(v1+v2)/2)`，并由 `P1->P2` 得到方向线。端点交换只会让箭头反向，不会改变中点；方向评价采用无向轴。
5. **在原图上绘制。** 使用如下固定视觉图例，任何人看同一张图都能判断结果：

   | 图层 | 颜色/形状 | 表示什么 |
   |---|---|---|
   | 半透明掩码 | 绿色 | 模型认为属于竹条的区域。 |
   | 两个端点 | 蓝色实心圆，标 `P1`、`P2` | 竹条两端的预测位置。 |
   | 端点连线 | 黄色细线 | 竹条主轴；用于迅速发现端点落到背景或吸盘上的情况。 |
   | 中点 | 红色圆圈加十字，标 `C` | 两端平均得到的目标中点。 |
   | 方向 | 从 C 沿主轴的黄色短箭头 | 仅表示轴向，箭头朝向本身没有“正确左右”。 |
   | 状态文字 | 左上角 | `frame_id`、时间戳、`q`、像素中心、模型/标定版本。 |
   | 拒绝状态 | 红色叉与 `INVALID: 原因` | 未检测到、遮挡、低置信度或几何检查失败；禁止画出伪中点。 |

6. **保存可审计结果。** 每个样本保存“原图、叠加图、JSON 数值”三件套，例如写到临时预览目录 `/tmp/aubo_geometry_preview/`；在正式数据阶段再写入独立 `reports/bamboo_geometry_v1/`。不能只弹出一个窗口后丢失结果。
7. **人工验收。** 先抽查至少 30 个静态、互不相同的竹条摆放：逐张看红色 `C` 是否位于实际中部、蓝点是否在两端、黄线是否沿竹条；记录“通过/失败原因”，而不是凭印象说“看起来不错”。
8. **再做数值验收。** 在带可见中心标记或已量过真值的测试图上，计算红色 `C` 与真值的像素误差；标定完成后将它换算为桌面平面的毫米误差。只有数值门槛通过，才允许进入阶段 3。

#### 一张合格叠加图应是什么样子

```text
原始画面中的竹条：     ●────────────────●
叠加后的含义：          P1               P2        （蓝色端点）
                         \_______________/          （黄色轴线）
                                  ⊕ C                 （红色中点）
```

若竹条被吸盘遮住一端、画面里有两根相似竹条、反光让掩码断裂，正确行为是显示红色 `INVALID`，等待人工检查或重新采图；**不是**沿用上一帧中点、猜一个中点，更不能据此移动机器人。

#### 预计的最小新增文件（未来实施时，当前不存在）

| 文件 | 单一职责 | 禁止事项 |
|---|---|---|
| `geometry_perception.py` | 从单张 `handeye` 图像输出 mask、端点、中点、方向、q 和拒绝原因 | 不连接 AUBO，不发送动作。 |
| `geometry_preview.py` | 读取离线图像/只读相机帧，生成叠加 PNG 与 JSON | 不调用 `move_to_start.py`、`evaluate*.py` 或遥操。 |
| `geometry_evaluate.py` | 对带真值的独立静态集计算像素/毫米误差报告 | 不改写原始数据集。 |

实现前应先为“端点顺序交换、端点越界、空掩码、低置信度、RGB/BGR 输入”编写离线单元测试；这些测试只使用本地图片，不涉及真实硬件。

## 5. 分阶段实施计划与交付门槛

### 阶段 0：冻结对照与度量定义（只读，不控制机器人）

**目标：**明确“成功”和“中点误差”是什么，避免之后只看训练 loss。

1. 记录纯 ACT 基线的模型版本、数据版本、相机位置、光照、起始位和测试结果。
2. 测量吸盘在当前竹条表面上的有效容许半径 `r_eff`：这是“离中点多远仍可稳定吸住”的工程量，不以标称直径替代。
3. 定义几何验收阈值：建议以 `P95(center_error) <= 0.5*r_eff` 为进入机器人闭环测试的门槛；具体毫米数待 `r_eff` 实测后填写。
4. 制作 30～50 个独立静态测试摆放：覆盖训练区内与边界处的位置、角度、亮度；每个位置用可见中心标记或精确量具给出真值。此集不得作为感知训练集。

**交付物：**`metrics_definition.md`、真值表、纯 ACT 对照记录。
**通过条件：**没有“成功率”或“中点”含义含糊的指标。

### 阶段 1：相机—机器人—桌面标定

**目标：**使 `handeye` 的像素坐标可以转换为 AUBO 基座坐标。

1. **内参标定：**相机固定、分辨率固定为当前 640×480、焦距/对焦固定；采集多姿态棋盘格或 ChArUco 图像，估计相机矩阵和畸变参数。
2. **外参标定（眼在手外）：**在桌面上设置已知、固定的机器人基座参考点/标定板，求 `T_base_camera`。这不是传统“手眼相机随末端移动”的假设；需按眼在手外固定相机关系处理。
3. **桌面平面：**在基座坐标系测量至少 3 个不共线桌面点，拟合平面；记录日期、工具、点位和残差。
4. **复验：**将若干已知棋盘角或桌面标记从像素反投影到基座系，与实际测量比较。

**交付物：**版本化标定文件、坐标系图、重投影误差与桌面点位残差报告。
**通过条件：**重新启动相机后复验仍通过；相机支架、分辨率、焦距或位置任一改变即判定标定失效并重新标定。

### 阶段 2：竹条端点/中点感知（离线、无机械臂动作）

**目标：**从 `handeye` 图像稳定输出端点、中点、方向和置信度。

1. 从历史视频和新采集的静态画面抽取图片。训练集应覆盖背景、竹条横纵位置、角度、光照、阴影、吸盘遮挡和不同竹条表面；测试集按“摆放位置”切分，不能随机抽相邻视频帧。
2. 对每张图标注“竹条掩码 + 两端点”；端点在遮挡时标记为不可见，而不是猜测真值。
3. 原型顺序：
   - 先实现传统颜色/边缘/直线拟合基线，用来暴露光照和反光问题；它不是最终方案。
   - 再训练轻量分割或关键点网络；从掩码主轴和投影极值获得端点。SAM 可用于辅助人工标注、生成候选掩码，但不能未经本场景量化就作为实时可靠检测器 [6]。
4. 按第 4.4 节的固定图例逐帧输出调试叠加图：掩码、端点、中点、方向、置信度和拒绝原因；保存而非只在窗口中显示。

**评估指标：**

| 指标 | 含义 | 阶段 2 建议门槛 |
|---|---|---|
| 中点像素误差 / 平面毫米误差 | 预测中点相对独立真值 | 报告均值、P50、P95；进入下一阶段需满足阶段 0 的 `P95` 门槛。 |
| 方向误差 | 忽略 180° 后的最小夹角 | 报告 P95；先以 5° 为工程目标，未达标须检查端点/标定。 |
| 检出率与误检率 | 应检测时是否给出正确竹条 | 分场景报告，不能只报总平均。 |
| 置信度校准 | 高 q 是否真的对应低误差 | 将 q 分桶，检查高 q 样本的 P95 误差。 |
| 时序抖动 | 静止竹条时连续帧中点/方向的标准差 | 与 25 Hz 控制周期共同报告；不得仅靠强平滑掩盖错误检测。 |

**通过条件：**独立静态测试集满足中心误差门槛，且低置信度场景会被可靠标记出来。此阶段绝不向 AUBO 下发动作。

### 阶段 3：记录几何状态与建立“几何增强数据集”

**目标：**使训练与推理拥有同定义、同时间基准的几何状态。

1. 新建数据集版本，例如 `bamboo_geometry_s01`，不覆盖 `bamboo_newview_s01`～`s04` 或 `bamboo_newview_full`。
2. 在每个 25 Hz observation 时间戳，记录原始两路图像和固定顺序的 19 维 `observation.state`（原有 13 维 + 6 维几何量），并保存检测器/标定版本和置信度说明。
   这是固定长度的 G1 数据集；不同长度的 G2 必须使用 20 维状态和新的独立数据集，例如 `bamboo_geometry_length_s01`，不与 G1 混写。
3. 同时离线处理历史 30 条视频作为**探索性**数据；只有在图像时间戳和结果完全可复现、并通过阶段 2 审核时，才可建立单独的“历史回填版本”。不能静默改写原数据集。
4. 录制期间继续筛掉抓空、碰撞和明显犹豫示教；几何状态不是把低质量动作标签变成高质量标签的魔法。
5. 额外记录关键事件：吸盘由 0→100 的帧、接触前末端位姿、放置与释放帧。计算 `grasp_offset = tcp_contact_xy - C_B.xy`，统计每位操作者/每批次的系统偏差与方差。

**通过条件：**随机抽查录制与离线重放的同一帧，几何 feature 一致；无 NaN、无坐标系混用、没有以零向量伪装不可见。

### 阶段 4：几何状态增强 ACT（离线训练与消融）

**目标：**只回答“几何状态是否让策略更稳定”，不混入控制器效果。

实验必须至少包含下列三组，训练步数、数据切分、相机和评价摆放保持一致：

| 实验 | 输入 | 用途 |
|---|---|---|
| E0 | 当前纯 ACT：图像 + 13 维状态 | 固定对照。 |
| E1 | E0 的 `observation.state` 由 13 维扩为 19 维，末 6 维为正确几何量 | 验证几何状态的真实增益。 |
| E2 | E1，但将几何状态置零或打乱 | 检查 E1 是否只是偶然波动，或策略是否实际使用了几何信息。 |

不同长度的 G2 通过前述 G1 审核后再单独建立：其对照组仍为 E0/E1 思路，但状态为 20 维，并额外按长度档报告结果；不要在同一次训练中混入“首次引入几何状态”和“首次引入长度变化”两个因素。

训练数据和验证数据必须按完整 episode、并按竹条摆放位置切分。除验证 loss 外，至少报告“进入抓取阶段时 TCP 到几何中点的距离分布”“吸盘事件时的抓取偏差”和独立真实机器人成功率。模仿学习的状态分布会受策略自身动作影响，单纯离线行为克隆可能遇到误差累积；需要把失败状态和人工恢复后的观测专门记录，逐步形成闭环纠错数据 [7]。

**通过条件：**E1 在独立位置上的中点接近误差与真实抓取成功率均优于 E0，且没有通过更激进的安全边界/动作后处理获得“虚假提升”。

### 阶段 5：可选的共享自治对中（独立分支，最后执行）

这不是本计划的默认交付。只有阶段 4 已经证明感知和状态增强有效后，才讨论：操作者将 TCP 移到竹条上方的安全高度，按住明确的“对中”按钮后，系统在限定 XY 小范围内闭环对准；下降、吸取、搬运和放置仍由操作者或独立策略完成。

该分支必须具备：现场人员与急停、显式使能/松手即停、速度/位移/工作区限制、置信度低即拒绝执行、控制日志、故障注入测试和独立模型/数据目录。它应被命名为“几何辅助/共享自治”，而不是纯 ACT。

## 6. 对手机遥操误差的具体处理

手机遥操误差分为两种，处理方式不同：

1. **感知误差：**不知道竹条在哪里。由阶段 1～2 的标定与端点检测解决，并以真值误差衡量。
2. **示教标签误差：**知道中点，但人没有对准、太早吸取或拖动竹条。不能靠往 ACT 输入中点彻底修复，必须通过数据策略降低。

建议的数据采集策略是“**人负责任务流程，几何负责测量和审计**”：

- 演示时显示中点叠加层，但不自动控制机器人；操作者可据此提高示教一致性。
- 将抓取偏差分档：高精度成功、普通成功、失败/接触异常。第一轮 E1 只用高精度成功示教训练抓取阶段；普通成功保留用于分析，不要无标记混入。
- 当策略出现偏离时，在安全现场由人恢复并录制该状态；这比只重复理想起点的数据更有助于减轻部署状态分布偏移 [7]。
- 若将来启用共享自治，对中片段必须带 `assist_mode=true` 标签，并与纯人工示教分开比较。

## 7. 风险、失效模式与防护

| 风险 | 可能现象 | 防护与判定 |
|---|---|---|
| 相机移动/焦距或曝光变化 | 中点整体偏移，旧标定失效 | 将相机固定与参数版本写入标定文件；变化即重新标定和复验。 |
| 端点被吸盘或手遮挡 | 方向跳变、误报中点 | 输出低置信度/不可见，不用上一帧坐标强行冒充当前测量。 |
| 竹条弯曲、离开桌面 | 单目平面投影产生系统误差 | 明确适用范围；超出范围转入 RGB-D/双目评估。 |
| 端点顺序交换 | `theta` 瞬间翻转 180° | 用 `cos(2θ), sin(2θ)` 表示无向轴。 |
| 标定坐标系混淆 | 米/毫米或 camera/base 变换错误，可能导致危险动作 | 文件名、字段名和单元测试中强制标注 frame 与单位；先离线叠加验证。 |
| 置信度没有意义 | 模型自信但错误 | 用独立集做 q 分桶校准；低 q 只能触发拒绝/提示，不触发补偿动作。 |
| 只看训练 loss | 误以为抓取更准 | 以中点误差、抓取偏差、位置外测试成功率为主指标。 |
| 偷偷加入任务脚本 | “效果提升”但不再是 ACT 能力 | E0/E1/共享自治分开版本、目录、日志与报告。 |

## 8. 工程实现影响范围（仅预估，当前不修改）

| 组件 | 预计增量 | 不应修改的部分 |
|---|---|---|
| 新感知模块 | 标定加载、端点/掩码检测、平面投影、可视化与离线评估 | 不接入机器人发送动作。 |
| 录制脚本 | 将 6 维几何量以固定顺序追加到 `observation.state`，使其由 13 维变为 19 维；写入版本元数据并保留原始图像/action | 不改变手机到绝对末端 action、吸盘 0/100 语义或安全 processor。 |
| 数据聚合/审核 | 校验几何 feature 维度、有限值、坐标系版本、置信度分布 | 不重建或覆盖当前 `bamboo_newview_full`。 |
| 训练/推理 | 从 dataset metadata 读取 19 维 state；训练与推理共享同一 geometry processor 和状态顺序校验 | 不用硬编码中点阈值覆盖 ACT 输出。 |
| 评估 | 保存叠加图、几何误差、成功/失败原因 | 不自动启动真实机械臂运动。 |

推荐采用独立目录和版本，例如：

```text
bamboo_geometry_s01/                  # 新录制批次
bamboo_geometry_full_v1/              # 已审核的聚合版本
models/bamboo_geometry_act_run01/     # E1 模型
reports/bamboo_geometry_v1/            # 标定、感知、消融和现场测试报告
```

回滚方式很简单：E0 继续加载现有 `bamboo_newview_full` 与纯 ACT checkpoint；几何实验失败时不删除任何旧数据或模型，只停止该独立分支。

## 9. 审核结论与下一步决策

### 可行性结论

- **技术上可行：**固定外置 RGB 相机 + 桌面平面足以在“竹条平放、无遮挡、相机固定”的受控场景中估计基座系中点；不需要先购买深度相机。
- **工程上可控：**将感知、标定、记录、训练和共享自治严格分阶段，可在任何门槛失败时停在只读层，不影响当前纯 ACT 链路。
- **不应承诺的结论：**在尚无标定误差、独立中点误差和真实抓取统计前，不能承诺“精准抓住中点”或给出成功率。

### 推荐决策

批准顺序应为：**阶段 0 → 阶段 1 → 阶段 2 的离线叠加验证 → 单独评审**。只有评审通过才进入阶段 3。不要现在直接给 ACT 增加状态、重训，或启动视觉伺服。

### 需要用户在实施前确认的事项

1. 竹条是否始终平放在同一桌面，允许的翘起高度和弯曲程度是多少？
2. 当前吸盘的有效容许半径 `r_eff` 实测是多少？若未测量，应先测量。
3. 是否允许在桌面上临时放置标定板/中心标记，以及是否可采集 30～50 个不带机器人动作的静态标定/测试画面？
4. 第一阶段的目标是“几何状态增强 ACT”，还是未来明确需要“按键触发的共享自治对中”？二者的安全审查与实验定义不同。

## 10. 参考文献与资料

1. Zhao, T. Z. et al. (2023). *Learning Fine-Grained Bimanual Manipulation with Low-Cost Hardware*（ACT）. [arXiv:2304.13705](https://arxiv.org/abs/2304.13705).
2. Manuelli, L. et al. (2019). *kPAM: KeyPoint Affordances for Category-Level Robotic Manipulation*. [arXiv:1903.06684](https://arxiv.org/abs/1903.06684).
3. Chaumette, F., & Hutchinson, S. (2006). *Visual Servo Control, Part I: Basic Approaches*. IEEE Robotics & Automation Magazine, 13(4), 82–90. [DOI:10.1109/MRA.2006.250573](https://doi.org/10.1109/MRA.2006.250573).
4. Zhang, Z. (2000). *A Flexible New Technique for Camera Calibration*. IEEE TPAMI, 22(11), 1330–1334. [DOI:10.1109/34.888718](https://doi.org/10.1109/34.888718).
5. OpenCV. *Camera Calibration and 3D Reconstruction (`calib3d`)*. [官方文档](https://docs.opencv.org/4.4.0/d9/d0c/group__calib3d.html).
6. Kirillov, A. et al. (2023). *Segment Anything*. [arXiv:2304.02643](https://arxiv.org/abs/2304.02643).
7. Ross, S., Gordon, G., & Bagnell, J. A. (2011). *A Reduction of Imitation Learning and Structured Prediction to No-Regret Online Learning*（DAgger）. AISTATS / PMLR 15. [论文页](https://proceedings.mlr.press/v15/ross11a.html).
8. Zeng, A. et al. (2020). *Transporter Networks: Rearranging the Visual World for Robotic Manipulation*. CoRL. [arXiv:2010.14406](https://arxiv.org/abs/2010.14406). 本文作为“显式空间表征可提升视觉抓取学习”的备选路线参考，不建议在第一期替换现有 ACT。
